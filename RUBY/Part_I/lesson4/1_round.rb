# frozen_string_literal: true

pi = Math::PI

#  methods that can round a number :

puts " Rounding of pi  = #{pi}:
        > using round: #{pi.round(2)}
        > using ceil: #{pi.ceil(2)}
        > using floor: #{pi.floor(2)}
        > using truncate #{pi.truncate(2)}"
